version: '3.8'

services:
  es01:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.10.2
    container_name: es01
    environment:
      - discovery.type=single-node # Apenas um nó
      - xpack.security.enabled=true # Desativa a necessidade de certificados para comunicação. Apenas para desenvolvimento!!!
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - esdata01:/usr/share/elasticsearch/data

volumes:
  esdata01:
    driver: local